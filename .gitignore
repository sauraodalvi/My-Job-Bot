__pycache__
.pytest_cache
.vscode
.idea

all excels
all resumes
logs

old

secrets.py
config/personals.py
.venv
apikeys.py

# Local control-panel runtime files — contain your credentials/answers, never commit
user_config.json
.bot_run.log
.bot_run.pid

# Built artifact
AutoJobApplier.exe
/build/
/dist/
*.spec

# Bot run & diagnostic artifacts
.bot_run.out.log
.bot_run.err.log
.diag_*.log
.diag_*.err.log
_diag_*.py
_diag_*.html
_diag_*.png
_diag_*.json

# Referral-finder runtime (logs, PID files, results, message history)
.referral_*.log
.referral_*.pid
referral_send_*.log
referral_usage.json
referral_results.json
referral_message_log.csv
referral_msg_usage.json
referral_job_log.json
usage.json

# Config backups — contain credentials; keep them off git entirely
user_config.json.bak-*

# Local dev helpers & misc artifacts
.serve_tmp.py
se-metadata.json

# Unused/abandoned local dependencies
node_modules/
package.json
package-lock.json

# Marketing & purchased-content collateral (kept out of the repo)
gumroad_content/
gumroad_content.zip
setup_folder/
landing page/
LANDING_COPY_PLAN.md

# AI question-answer cache (auto-generated, contains personal answers)
question_answer_cache.json
